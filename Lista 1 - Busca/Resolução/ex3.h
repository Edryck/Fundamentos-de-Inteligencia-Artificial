#ifndef EX3_H
#define EX3_H

#include <stdio.h>
#include <string.h>
#include <time.h>

// Executa a Questao 3a: coloracao de mapa com 4 cores,
// usando busca com backtracking. 
void run_ex3a(void);

// Executa a Questao 3b: problema dos jarros de 12L, 8L e 3L,
// buscando medir exatamente 1 litro (busca em largura). 
void run_ex3b(void);

// Executa a Questao 3c: macaco e as bananas, com dois caixotes
// que precisam ser empilhados para alcancar a altura necessaria
// (busca em largura sobre o espaco de estados). 
void run_ex3c(void);

#endif
